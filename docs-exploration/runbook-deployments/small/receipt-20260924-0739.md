VERIFIED
⚠ Deviated from the runbook: Used Cloud Build fallback when local Docker daemon is unavailable, enabled Uniform Bucket-Level Access on the GCS state bucket with storage.objectAdmin IAM bindings, and exported KUBECONFIG to bin/KUBECONFIG.

## Attempt Log

### Attempt 1
- **What failed:** GCS state store bucket authentication failure (`HTTP 412: The type of authentication token used for this request requires that Uniform Bucket Level Access be enabled`).
- **Evidence:** `kops create cluster` failed reading `gs://kops-state-as-small/as-small.k8s.local/config` with HTTP 412.
- **What changed:** Updated `deploy.sh` to create the GCS bucket with `--uniform-bucket-level-access`.

### Attempt 2
- **What failed:** GCS object access denied (`HTTP 403: Caller does not have storage.objects.get access... Permission 'storage.objects.get' denied`).
- **Evidence:** Workload identity principal `principal://iam.googleapis.com/...` needed explicit `roles/storage.objectAdmin` on the UBLA-enabled bucket.
- **What changed:** Added automatic IAM policy binding in `deploy.sh` granting `roles/storage.objectAdmin` on `gs://${BUCKET_NAME}` to active owner principals.

### Attempt 3
- **What failed:** E2E benchmark tests failed to locate kubeconfig at `bin/KUBECONFIG` and encountered missing dirty tag for `chrome-sandbox`.
- **Evidence:** `stat /workspaces/agent-sandbox/bin/KUBECONFIG: no such file or directory` and `rpc error: code = NotFound desc = failed to pull ... chrome-sandbox:v20260924-v1.0.3-37-g2b2885a-dirty`.
- **What changed:** Exported admin kubeconfig explicitly to `bin/KUBECONFIG`, set `KUBECONFIG` environment variable, and added image tags (`latest` and `-dirty` aliases). Re-ran `deploy.sh` and full E2E benchmark suite — all benchmarks passed.

---

## Verify Evidence

### 1. Cluster Nodes (`kops validate cluster` and `kubectl get nodes -o wide`)
All 4 nodes (1 control plane + 3 workers) are in `Ready` state:
```
NAME                               STATUS   ROLES           AGE   VERSION   INTERNAL-IP   EXTERNAL-IP      OS-IMAGE             KERNEL-VERSION            CONTAINER-RUNTIME
control-plane-us-central1-a-4g1f   Ready    control-plane   11m   v1.36.4   10.0.16.6     35.239.33.90     Ubuntu 24.04.4 LTS   6.17.0-1018-gcp (amd64)   containerd://2.2.4
nodes-us-central1-a-3sdc           Ready    node            10m   v1.36.4   10.0.16.3     136.115.182.24   Ubuntu 24.04.4 LTS   6.17.0-1018-gcp (amd64)   containerd://2.2.4
nodes-us-central1-a-cch3           Ready    node            10m   v1.36.4   10.0.16.5     136.64.119.246   Ubuntu 24.04.4 LTS   6.17.0-1018-gcp (amd64)   containerd://2.2.4
nodes-us-central1-a-lm8t           Ready    node            10m   v1.36.4   10.0.16.4     35.225.177.32    Ubuntu 24.04.4 LTS   6.17.0-1018-gcp (amd64)   containerd://2.2.4
```

### 2. Controller and Workload Pods (`kubectl get pods -A`)
`agent-sandbox-controller` and supporting system pods (Cilium, CoreDNS, GCE PD CSI Driver, Node Exporter) are healthy and `Running`:
```
NAMESPACE              NAME                                                       READY   STATUS    RESTARTS   AGE
agent-sandbox-system   agent-sandbox-controller-67f7cc4d48-xdpnk                  1/1     Running   0          9m
kube-system            cilium-bf8r5                                               1/1     Running   0          10m
kube-system            cilium-bgght                                               1/1     Running   0          10m
kube-system            cilium-f29hl                                               1/1     Running   0          10m
kube-system            cilium-pjpzw                                               1/1     Running   0          11m
kube-system            node-exporter-42hhm                                        1/1     Running   0          9m
kube-system            node-exporter-4sv6r                                        1/1     Running   0          9m
kube-system            node-exporter-cvcth                                        1/1     Running   0          9m
kube-system            node-exporter-kccnr                                        1/1     Running   0          9m
```

### 3. E2E Benchmarks Verification (`./dev/tools/test-e2e --suite=benchmarks`)
```
BenchmarkChromeSandboxClaimStartup-4   	       1	  38154070 ns/op	         0.03808 claim-ready-sec/op
BenchmarkChromeSandboxStartup-4        	       1	2005213121 ns/op	         1.867 chrome-ready-sec
BenchmarkWarmPoolParallelClaim-4       	       1	 210688886 ns/op	         0.2017 sec/claim-10-sandboxclaims-e2e
PASS
ok  	sigs.k8s.io/agent-sandbox/test/e2e	29.633s
PASS
ok  	sigs.k8s.io/agent-sandbox/test/e2e/extensions	0.035s
PASS
ok  	sigs.k8s.io/agent-sandbox/test/e2e/framework	0.032s
```

---

## What is Left Running (Teardown Contract)

The following resources were provisioned and are actively running in GCP project `barni-cnrm-20260529`:
1. **kOps Cluster:** `as-small.k8s.local`
   - 1x Control Plane VM (`control-plane-us-central1-a-4g1f`, `n2-standard-4`, zone `us-central1-a`)
   - 3x Worker Node VMs (`nodes-us-central1-a-3sdc`, `nodes-us-central1-a-cch3`, `nodes-us-central1-a-lm8t`, `n2-standard-4`, zone `us-central1-a`)
   - Associated GCE PD-SSD boot disks, VPC subnet `us-central1`, target pools, and firewall rules.
2. **GCS State Store Bucket:** `gs://kops-state-as-small` (location `us-central1`, label `repo-agent-instance=as-small`).
3. **Container Images:** `gcr.io/barni-cnrm-20260529/as-small/agent-sandbox-controller` and `gcr.io/barni-cnrm-20260529/as-small/chrome-sandbox`.

Execute `./docs-exploration/runbook-deployments/small/teardown.sh` to delete the cluster and GCS state bucket.

---

## Cost-Relevant Resources Created
- **GCE Compute VMs:** 4x `n2-standard-4` instances in `us-central1-a`.
- **GCE Persistent Disks:** 4x `pd-ssd` root volumes.
- **GCS Storage:** 1x bucket (`gs://kops-state-as-small`) storing kOps cluster state.
- **Cloud Build:** 1 build execution for image generation.

---

## Recommended Runbook Changes

### 1. Requirements & IAM Permissions
- **Uniform Bucket-Level Access (UBLA):** GCS state store bucket creation must specify `--uniform-bucket-level-access` when running in federated/WIF environments.
- **Storage IAM:** Principal requires `roles/storage.objectAdmin` on the state store bucket.

### 2. Suggested Step Adjustments

#### Step 4 Replacement:
```sh
### 4. Create the GCS State Store Bucket
gcloud storage buckets create "gs://${BUCKET_NAME}" \
  --project="${PROJECT_ID}" \
  --location="${REGION}" \
  --uniform-bucket-level-access

gcloud storage buckets update "gs://${BUCKET_NAME}" \
  --update-labels="repo-agent-instance=${RESOURCE_PREFIX}" || true

# Grant storage.objectAdmin on the state bucket to active identity
PRINCIPALS=$(gcloud projects get-iam-policy "${PROJECT_ID}" --filter="bindings.role:roles/owner" --flatten="bindings[].members" --format="value(bindings.members)")
for principal in ${PRINCIPALS}; do
  gcloud storage buckets add-iam-policy-binding "gs://${BUCKET_NAME}" --member="${principal}" --role="roles/storage.objectAdmin" &> /dev/null || true
done
```

#### Step 8 Replacement:
```sh
### 8. Export Kubeconfig
mkdir -p bin
kops export kubeconfig --name="${CLUSTER_NAME}" --admin --kubeconfig="bin/KUBECONFIG"
kops export kubeconfig --name="${CLUSTER_NAME}" --admin
export KUBECONFIG="$(pwd)/bin/KUBECONFIG"
```
